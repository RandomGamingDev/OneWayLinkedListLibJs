{
  "name": "one-way-linked-list",
  "version": "1.0.5",
  "description": "A simple one way linked list that stores the head, tail and length of the list",
  "main": "list.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "randomgamingdev",
  "license": "MIT"
}
